# 图书管理系统 · 系统技术文档

记录这个系统的架构设计、数据结构选型理由，
以及**本人负责的三个核心模块**的实现细节。

> 本项目为**课程小组作业**，由 8 位同学协作完成。
> 原始仓库：[GK-6660/JC1503-Library-System](https://github.com/GK-6660/JC1503-Library-System)
>
> 本人负责：**哈希表、双向链表、栈**（详见第四节）

---

# 一、这个项目做了什么

**目标**：在**不使用 Python 内置容器**（list / dict）作为核心算法的前提下，
手写数据结构实现一个完整的图书管理系统。

这是课程的核心约束——**逼着我们理解数据结构的原理**，而不是会调 API。

**成果**：

| 指标 | 数值 |
|---|---|
| 代码量 | 1668 行 Python |
| 手写数据结构 | 6 个（哈希表、双向链表、栈、BST、队列、通用树） |
| 单元测试 | 25 个用例，全部通过 |
| 协作者 | 8 人 |
| 文档 | LaTeX 报告 + README |

**功能**：用户管理、图书/杂志管理、借阅归还、预约排队、操作撤销、数据持久化。

---

# 二、系统架构

## 分层设计

```
┌─────────────────────────────────────────────────┐
│  入口层    main.py                               │
│           命令行菜单、输入解析、异常捕获           │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  业务层    models/                               │
│           Resource（抽象基类）                    │
│             ├── Book                            │
│             └── Magazine                        │
│           User（持有借阅记录链表）                │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  数据层    structures/                           │
│           哈希表 │ 双向链表 │ 栈                  │
│           二叉搜索树 │ 队列 │ 通用树               │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  工具层    utils/                                │
│           自定义异常体系 │ JSON 持久化            │
└─────────────────────────────────────────────────┘
```

## 数据在这三层之间怎么流动

```
用户输入 "U001"
    ↓
main.py 解析 → self.users.get("U001")
    ↓
HashTable.get() → 哈希函数算出桶号 → 遍历桶内链表 → 返回 User 对象
    ↓
User.borrow_book("Book_Title_1")
    ↓
user.borrowed_items.append("Book_Title_1")   ← 双向链表
    ↓
history_stack.push({...})                     ← 栈（撤销用）
```

---

# 三、关键技术决策

## 决策 1：为什么用哈希表存用户

**需求**：按用户 ID 查找用户，这是**最高频的操作**（借书、还书、删除都要先查用户）。

| 数据结构 | 查找复杂度 | 评价 |
|---|---|---|
| 数组 / 链表 | O(n) | 30 个用户就要平均比较 15 次 |
| **哈希表** | **O(1)** 平均 | ✅ 选它 |

用户 ID 是**离散的字符串**（U001、U002…），非常适合哈希索引。

## 决策 2：为什么用 BST 存图书

**需求**：图书要支持**按标题查找**、还要能**按字母顺序遍历**（打印书单）。

| 数据结构 | 查找 | 有序遍历 |
|---|---|---|
| 哈希表 | O(1) | ❌ 无法按顺序输出 |
| **二叉搜索树** | **O(log n)** | ✅ **中序遍历天然有序** |

图书标题是**有顺序意义的字符串**，用 BST 能在支持查找的同时保留排序能力。
这是**根据数据特性选择结构**的例子。

## 决策 3：为什么用队列处理预约

**需求**：书被借完时，排队等待；有人还书时，**先排队的人先拿到**。

**先进先出**——这正是队列的定义。

## 决策 4：为什么用栈做撤销

**需求**：撤销**最近一次**操作。

**后进先出**——栈的定义。每执行一个操作就 push 一条记录，撤销时 pop。

## 决策 5：为什么借阅记录用链表

**需求**：用户的借还书是**频繁的插入和删除**。

| | 数组 | 链表 |
|---|---|---|
| 尾部插入 | O(1) | O(1) |
| **中间/头部删除** | **O(n)**（要搬移元素） | **O(1)**（改指针） |

用双向链表，删除时能**直接拿到前驱节点**，不用从头遍历。

---

# 四、★ 核心数据结构实现

**以下三个模块由本人实现。**

## 4.1 哈希表（`src/structures/hash_table.py`）

**用途**：按用户 ID 快速索引 User 对象。

### 结构设计

```
self.table = [None] * 100        # 100 个桶
         ↓
    ┌────┬────┬────┬────┬─────┐
    │ 0  │ 1  │ 2  │ …  │ 99  │
    └─┬──┴────┴─┬──┴────┴─────┘
      │         │
      ▼         ▼
   HashNode   HashNode → HashNode → None
   (U001)     (U015)     (U031)      ← 冲突时挂成链表
```

### 哈希函数

```python
def hash_func(self, key_str):
    total_ascii_sum = 0
    for char_in_key in key_str:
        total_ascii_sum += ord(char_in_key)   # ord() 把字符转成 ASCII 码
    return total_ascii_sum % self.capacity    # 取模映射到桶号
```

**思路**：把字符串的每个字符的 ASCII 码累加，再对桶数取模。

> **已知的局限**：求和法对**字母异位词**（如 "AB" 和 "BA"）会算出同一个桶号。
> 这不是 bug——它们会走冲突处理逻辑正常存储，只是分布不够均匀。
> 更好的做法是多项式滚动哈希（如 `h = h*31 + c`），已在下方"可延伸方向"中记录。

### 冲突解决：链地址法

```python
def insert(self, key_to_insert, value_to_insert):
    idx = self.hash_func(key_to_insert)

    # 情况一：桶是空的，直接放
    if self.table[idx] == None:
        self.table[idx] = HashNode(key_to_insert, value_to_insert)
        self.size += 1
        return

    # 情况二：桶里已有节点，沿链表走到末尾
    prev_node = None
    temp_node = self.table[idx]
    while temp_node != None:
        if temp_node.key == key_to_insert:
            raise DuplicateItemError("Exists")   # 顺便查重
        prev_node = temp_node
        temp_node = temp_node.next

    prev_node.next = HashNode(key_to_insert, value_to_insert)   # 挂到链尾
    self.size += 1
```

**为什么选链地址法而不是开放寻址法**：

| | 链地址法 | 开放寻址法 |
|---|---|---|
| 装填因子容忍度 | 可以 > 1 | 必须 < 1 |
| 删除操作 | **简单**（改指针） | **麻烦**（要做墓碑标记，否则查找链断裂） |
| 实现复杂度 | 低 | 高 |

**本项目删除用户是常见操作**（有 `delete_user` 功能），所以链地址法更合适。

### 删除：链表断链

```python
def remove(self, key_to_remove):
    idx = self.hash_func(key_to_remove)
    current = self.table[idx]
    prev = None

    while current != None:
        if current.key == key_to_remove:
            if prev == None:
                self.table[idx] = current.next    # 删的是头节点
            else:
                prev.next = current.next          # 删的是中间/尾节点
            self.size -= 1
            return
        prev = current
        current = current.next

    raise ItemNotFoundError("Not found")
```

**关键点**：删除头节点和删除中间节点要分开处理——头节点没有前驱，必须改 `table[idx]` 的指向。

### 复杂度

| 操作 | 平均 | 最坏（全冲突） |
|---|---|---|
| insert | O(1) | O(n) |
| get | O(1) | O(n) |
| remove | O(1) | O(n) |

---

## 4.2 双向链表（`src/structures/linked_list.py`）

**用途**：管理每个用户借阅的书（`User.borrowed_items`）。

### 结构设计

```
   first                                          last
     ↓                                             ↓
  ┌──────┐     ┌──────┐     ┌──────┐     ┌──────┐
  │ data │ ⇄   │ data │ ⇄   │ data │ ⇄   │ data │
  │ prev │     │ prev │     │ prev │     │ prev │
  │ next │     │ next │     │ next │     │ next │
  └──────┘     └──────┘     └──────┘     └──────┘
```

**同时保存头指针和尾指针**——这是关键设计。

### 尾部插入是 O(1)

```python
def append(self, val_to_add):
    new_node_obj = Node(val_to_add)

    if self.count == 0:              # 空链表
        self.first = new_node_obj
        self.last = new_node_obj
    else:                            # 直接挂到 last 后面
        old_last_node = self.last
        old_last_node.next = new_node_obj
        new_node_obj.prev = old_last_node
        self.last = new_node_obj

    self.count += 1
```

**如果没有尾指针**，每次追加都要从头遍历到尾，复杂度退化成 O(n)。
**保存 `self.last` 后，追加是 O(1)。**

### 删除要处理三种情况

```python
def remove(self, val_to_remove):
    temp_current_node = self.first

    while temp_current_node != None:
        if temp_current_node.data == val_to_remove:

            if temp_current_node == self.first:
                # 情况①：删头节点
                self.first = temp_current_node.next
                if self.first != None:
                    self.first.prev = None
                else:
                    self.last = None        # 链表空了，尾指针也要清
                print("Removed first item")

            elif temp_current_node == self.last:
                # 情况②：删尾节点
                self.last = temp_current_node.prev
                self.last.next = None
                print("Removed last item")

            else:
                # 情况③：删中间节点 —— 双向链表的优势所在
                prev_node = temp_current_node.prev
                next_node = temp_current_node.next
                prev_node.next = next_node
                next_node.prev = prev_node
                print("Removed middle item")

            self.count -= 1
            return True

        temp_current_node = temp_current_node.next

    return False
```

**③ 是双向链表的价值所在**：删中间节点时，**直接通过 `prev` 拿到前驱**，两行改指针就完成，O(1)。
单链表就只能从头再遍历一次去找前驱，O(n)。

**① 里最容易被忽略的一行**：删掉最后一个节点后，`self.last` 必须置空，
否则尾指针会指向一个已经被移除的节点。

### 实现的魔术方法

```python
def __len__(self):
    return self.count

def __iter__(self):
    current_pointer = self.first
    while current_pointer != None:
        yield current_pointer.data
        current_pointer = current_pointer.next
```

实现 `__len__` 后可以直接 `len(user.borrowed_items)`；
实现 `__iter__` 后可以直接 `for book in user.borrowed_items`——
**让自定义结构用起来和内置类型一样自然**，这是 Python 的鸭子类型思想。

> 这个 `__len__` 在 `main.py` 里被用到：删除用户前会检查
> `len(user_obj.borrowed_items) > 0`，**有未归还的书就不允许删除**。

---

## 4.3 栈（`src/structures/stack.py`）

**用途**：支撑管理员的「撤销」功能（`history_stack`）。

### 结构设计

```
   top_node
      ↓
   ┌──────┐    ┌──────┐    ┌──────┐
   │ 最近 │ →  │  ...  │ →  │ 最早 │ → None
   └──────┘    └──────┘    └──────┘
      ↑
   只在这里插入和删除
```

### 实现

```python
def push(self, item_to_push):
    new_stack_element = StackNode(item_to_push)

    if self.item_count == 0:
        self.top_node = new_stack_element
    else:
        new_stack_element.next = self.top_node    # 新节点指向原来的栈顶
        self.top_node = new_stack_element         # 栈顶指针上移
    self.item_count += 1

def pop(self):
    if self.item_count == 0:
        return None

    old_top_node_ref = self.top_node
    value_to_return = old_top_node_ref.value
    self.top_node = old_top_node_ref.next         # 栈顶下移
    self.item_count -= 1
    return value_to_return
```

**push 和 pop 都是 O(1)**——只在栈顶操作，不涉及遍历。

### 为什么栈适合做撤销

```
执行操作 → push 一条记录
撤销     → pop 最新的那条
```

**后进先出**正好对应"撤销最近一次操作"的语义。

### 存的是什么

栈里存的不是函数，而是**操作描述字典**：

```python
self.history_stack.push({
    "action_type": "borrow",
    "user_id": user_id_to_borrow,
    "item_title": item_title_to_borrow
})
```

撤销时 `pop` 出这个字典，根据 `action_type` 走对应的**反向逻辑**：

| 原操作 | 撤销时做什么 |
|---|---|
| `add_user` | 删除该用户 |
| `add_book` | 删除该图书 |
| `borrow` | 归还 + 处理等待队列 |
| `return` | 重新借出 |
| `delete_user` / `delete_book` | 恢复 |

**这是"命令模式"的朴素实现**——把操作记录成数据，让它可以被回放和逆转。

### 查看历史时的一次巧用

`show_history()` 要求**最新的操作显示在最前面**，但栈只能从栈顶弹出。
实现方式是**用两个栈倒腾**：

```python
temp_stack = Stack()
while self.history_stack.item_count > 0:
    action = self.history_stack.pop()
    temp_history_list.append(action)
    temp_stack.push(action)          # 暂存到临时栈

while temp_stack.item_count > 0:
    self.history_stack.push(temp_stack.pop())   # 再倒回来，恢复原栈
```

**既完成了遍历，又没有破坏原栈的状态。**

---

# 五、其他数据结构（组员实现）

| 结构 | 用途 | 关键点 |
|---|---|---|
| **二叉搜索树** | 按书名检索图书 | 删除要处理三种情况：叶子节点、单子节点、双子节点（找后继替换） |
| **队列** | 预约排队 | 书归还时自动把书分配给队首用户 |
| **通用树** | 图书分类层级 | 支持子树移动、路径查找、DFS 遍历 |

---

# 六、业务逻辑设计

## 借阅流程

```
输入 用户ID + 书名
    ↓
哈希表查用户 → 不存在则报 ItemNotFoundError
BST 查图书   → 不存在则报 ItemNotFoundError
    ↓
有库存？ ──是──> 库存-1，加入用户借阅链表，操作入栈
    │
    否
    ↓
询问是否加入等待队列 ──是──> 队列 enqueue
```

## 归还流程（自动分配）

```
库存+1
    ↓
该书的等待队列非空？
    ├── 是 ──> 队首用户出队 → 自动借给他 → 打印通知
    └── 否 ──> 结束
```

**这是队列的实际价值**——不只是先进先出，而是**自动化的资源再分配**。

## 异常体系

```python
class LibraryBaseException(Exception): pass
    ├── ItemNotFoundError     # 找不到用户/图书
    ├── DuplicateItemError    # 重复插入
    └── OutOfStockError       # 库存不足
```

**统一的基类让上层可以一次捕获所有业务异常**：

```python
except LibraryBaseException as e:
    print(f"操作失败: {e}")
```

---

# 七、数据持久化

**方案**：JSON 文件（`utils/storage.py`）

每个模型实现 `to_dict()` / `from_dict()`：

```python
# 保存
for user_obj in users.to_list():
    data["users"].append(user_obj.to_dict())

# 恢复
for user_dict in loaded["users"]:
    user_object = User.from_dict(user_dict)
    hash_table.insert(user_object.user_id, user_object)
```

**`from_dict` 用 `@classmethod` 实现**——它需要根据子类类型创建实例
（`Book.from_dict()` 返回 Book，`Magazine.from_dict()` 返回 Magazine）。

**序列化的顺序有意义**：恢复时要**重新 insert 进哈希表和 BST**，
不能直接恢复内部结构——因为哈希值依赖键、BST 依赖插入顺序。

---

# 八、项目的局限

| 局限 | 说明 |
|---|---|
| **命令行界面** | 交互体验一般，没有图形界面 |
| **JSON 文件存储** | 全量读写、无并发控制、数据量大时慢 |
| **哈希函数简单** | ASCII 求和法分布不够均匀（但冲突处理是完整的） |
| **无并发处理** | 单用户系统 |
| **扩容未实现** | 定义了 `load_factor_threshold = 0.7`，但没有自动 rehash 的逻辑 |

---

# 九、技术栈

| 层面 | 技术 |
|---|---|
| 语言 | Python 3 |
| 数据结构 | 手写哈希表、双向链表、栈、BST、队列、通用树 |
| 面向对象 | 继承、多态、抽象基类、类方法工厂、魔术方法 |
| 测试 | `unittest`（25 个用例，8 个测试类，含集成测试） |
| 持久化 | JSON 序列化 |
| 文档 | LaTeX 报告、Markdown |
| 协作 | Git 分支 + Pull Request（8 人） |

---

# 十、可延伸方向

| 方向 | 做法 |
|---|---|
| **Web 界面** | 用 Flask 包一层 REST API + 简单前端，把命令行换成网页 |
| **换数据库** | JSON 文件 → SQLite/MySQL，支持并发和索引 |
| **哈希函数优化** | ASCII 求和 → 多项式滚动哈希（`h = h*31 + c`），改善分布 |
| **自动扩容** | 装填因子超过阈值时 rehash 到两倍容量（`load_factor_threshold` 已预留） |
| **性能对比** | 手写结构与 Python 内置 `list`/`dict` 做 benchmark，量化差距 |

---

# 十一、文档索引

| 文档 | 内容 |
|---|---|
| [`README.md`](README.md) | 运行方式、功能菜单说明、操作指南 |
| [`src/main.py`](src/main.py) | 主程序（含菜单与业务逻辑） |
| [`src/structures/`](src/structures/) | 六个手写数据结构 |
| [`tests/test_main.py`](tests/test_main.py) | 25 个单元测试用例 |
| [`Latex/main.tex`](Latex/main.tex) | LaTeX 报告源码 |

---

# 十二、这段经历的价值

## 1. 理解原理，而不只是会调用

课程约束"禁用内置容器"看似麻烦，实际是**逼着理解底层**：

- 用 `dict` 一行搞定的事，手写要处理**冲突、扩容、删除断链**
- 用 `list` 一行搞定的事，手写要处理**前驱后继指针**

**知道 `dict` 内部是哈希表**和**亲手实现过哈希表**，是两种理解深度。

## 2. 根据数据特性选结构

| 需求 | 选择 | 理由 |
|---|---|---|
| 按 ID 快速查找 | 哈希表 | 离散字符串，只需查找 |
| 按标题查找 + 有序遍历 | BST | 需要排序能力 |
| 先到先得 | 队列 | FIFO 语义 |
| 撤销最近操作 | 栈 | LIFO 语义 |
| 频繁增删 | 双向链表 | O(1) 删除 |

**不是"哪个高级用哪个"，而是"哪个匹配需求"**。

## 3. 边界情况意识

写数据结构最花时间的不是主逻辑，而是**边界**：

- 空链表删除
- 删头节点 / 尾节点 / 中间节点
- 删到只剩一个节点时尾指针要清空
- 哈希冲突时既要插入又要查重
- 栈空时 pop 要返回 None 而不是崩溃

**这些在写业务代码时是隐藏的 bug，在写数据结构时会全部暴露出来。**

## 4. 团队协作

8 人分工，用 **Git 分支 + Pull Request** 流程：

- 每人负责独立模块，通过 PR 合并
- 通过 `git log` 可以追溯到每个人的贡献
- README 里明确了"禁止越权修改他人负责的文件"

---

> **本人负责模块的提交记录**（可在 git 历史中查证）：
> - `Update hash_table.py (#16)`
> - `Update linked_list.py (#13)`
> - `Update stack.py (#11)`
